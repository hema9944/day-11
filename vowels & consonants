#include<stdio.h>
#include<ctype.h>
int main(){
char str[1000];
int vowels = 0, consonants = 0;
printf("eneter a string: ");
fgets(str, sizeof(str), stdin);
for(int i = 0; str[i] != '\n'; ++i)
{
char4 ch = tolower(str[i]);
if(isalpha(ch))
{
if(ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || cj == 'u')
{
vowels++;
}
else {
consonants++;
}
}
}
printf("vowels: %d\n", vowels);
printf("consonants: %d\n",consonants);
return o;
}
